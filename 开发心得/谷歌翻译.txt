修改了样式。

顶部点出的语言 选择 面板、长按文本出现的 原文段落 面板，其宽度会超出文档宽度，但由于
position:fixed样式不影响文档宽度，导致无法横向滚动，超出部分无法阅读。
故修改布局方式为position:absolute，使其影响文档宽度、并允许横向滚动。

但是直接修改，会导致切换原文/翻译时 页面上下跳动，定位不准。
故监听dom节点变化而后动态修改或改回。

---

修改了操作逻辑。

输入框需要手动切换编辑/阅读模式。选中单词后，点击右上角半透明logo按钮，可点开单词释义。